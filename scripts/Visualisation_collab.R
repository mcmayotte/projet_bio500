library(igraph)
# Asigner une interaction à 10% des paires de noeuds
matrice_interact <- matrix(0, nr = 162, nc = 162)
for (i in 1:162) {
  x=runif(1)
  if (x>0.5) y=y+1
  else z=z+1 }

#matrice: ajout de titres de colonnes q
mafonction<-function(x in 1:162){
  if data[3,x]>1
  matrix[x,]
}
#test

L[runif(S*S) < C] = 1
sum(L)